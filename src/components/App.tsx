import * as React from 'react';
import styled from '../theme';

const App = () => <Container>TypeScript React!!</Container>;

const Container = styled.div`
  display: flex;
  flex-direction: column;
  justify-content: center;
  align-items: center;
`;

export default App;
